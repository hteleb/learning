server:
  port: 9000

external-service:
  version: v1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/company
    username: hteleb
    password: AlexinDallas2023
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    main:
      web-application-type: servlet